<%= form_for @hex, html: {multipart: true} do |f| %>
  <% if @hex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hex.errors.count, "error") %> prohibited this hex from being saved:</h2>

      <ul>
      <% @hex.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :hexcode %><br>
    <%= f.text_field :hexcode, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :notes %><br>
    <%= f.text_field :notes, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
